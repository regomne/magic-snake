# 二阶段开发计划

二阶段目标：把现有的“公式播放器”升级为可以直接设计、验证、保存和分享造型的魔尺工具。

## 1. 直接编辑魔尺

- 点击模型选择关节。
- 顺时针、逆时针及 180° 旋转。
- 模型操作与公式内容双向同步。
- 支持撤销、重做和一键拉直。
- 增加常用键盘快捷键。
- 优化手机端关节选择与旋转操作。

## 2. 碰撞检测

- 检测不同方块之间的几何相交。
- 区分最终造型碰撞和旋转路径碰撞。
- 标记发生碰撞的步骤及相关方块。
- 可配置为阻止现实中无法完成的旋转。
- 为公式导入结果提供完整的可行性检查。

碰撞检测是二阶段中优先级最高、技术量最大的一项。

## 3. 完善公式系统

- 将解析器整理为可扩展的格式适配层。
- 支持更多常见魔尺公式记法。
- 支持不同公式格式之间的转换。
- 支持公式文本及 JSON 文件导入、导出。
- 使用 URL 参数分享长度和公式，例如：

  ```text
  /snake?length=24&formula=1(1),3(-1),5(2)
  ```

- 提供复制分享链接功能。
- 使用浏览器本地存储自动保存编辑内容。

## 4. 造型管理

- 保存自定义造型。
- 支持名称、描述和标签。
- 自动生成 3D 缩略图。
- 提供常用示例造型。
- 支持最近打开和收藏。

## 5. 教学与拆解模式

- 为当前关节显示明确的旋转轴和方向箭头。
- 固定部分与转动部分使用不同视觉状态。
- 支持单独查看任意一步。
- 增加爆炸拆解视图。
- 自动生成步骤卡片和文字说明。

## 6. 导出功能

- 导出 PNG 图片，可选择透明背景。
- 导出分步骤教学图。
- 导出 GIF 或 WebM 动画。
- 导出包含公式与配置的 JSON 文件。
- 预留 GLB、STL 等 3D 格式导出能力。

## 7. 视觉与操作完善

- 提供不同配色和塑料材质方案。
- 支持背景颜色切换。
- 支持透视相机和正交相机。
- 增加正视图、侧视图和俯视图。
- 增加全屏模式。
- 完善触摸手势与无障碍键盘操作。

## 8. 工程优化

- 对 Three.js 相关代码进行拆包，降低首次加载体积。
- 优化 48、72 段魔尺的渲染和碰撞性能。
- 增加几何、动画及碰撞专项测试。
- 使用 Playwright 覆盖核心浏览器交互。
- 增加 PWA 离线运行能力。
- 对本地存储进行版本化，提供错误恢复机制。

## 推荐实施顺序

1. 直接编辑、公式同步、撤销和重做。
2. 最终姿态与旋转路径碰撞检测。
3. 本地保存、导入导出和 URL 分享。
4. 教学视图及步骤卡片。
5. PNG、教学图和动画导出。
6. 视觉、移动端和工程性能优化。

## 二阶段验收标准

用户不需要手写公式，也可以直接在 3D 模型上扭出一个现实可行的造型；系统能够同步生成公式、检测碰撞、撤销和重做操作，并将造型保存或通过链接分享。用户还可以导出完整的分步教学内容。
